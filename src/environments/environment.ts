// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyADmSsp1CAGynfkmrqKF5ozVJdN4HcG098",
    authDomain: "industry-store.firebaseapp.com",
    databaseURL: "https://industry-store.firebaseio.com",
    projectId: "industry-store",
    storageBucket: "industry-store.appspot.com",
    messagingSenderId: "583521460654",
    appId: "1:583521460654:web:50438a914bd0fb0a"
  }
}

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
